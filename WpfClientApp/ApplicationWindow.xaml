<Window x:Class="WpfClientApp.ApplicationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfClientApp.ViewModels" 
        d:DataContext="{d:DesignInstance Type=local:ApplicationVM}"
        mc:Ignorable="d"
        Title="Телефонная книжка" Height="600" Width="800">
    <Grid>
        <Button x:Name="ContactsButton" Content="Загрузить контакты" 
                Command="{Binding LoadContactsDbCommand}" 
                Width="200" Height="50" FontSize="20" Margin="5"/>


    </Grid>
</Window>
